# サイト品質改善 ラウンド5 実行手順（Codex 引き継ぎ用）

作成日: 2026-08-15
親文書: `docs/site-improvement-execution-plan.md`（**優先順位・制約・受け入れ条件はそちらが正**）
前ラウンド: `docs/site-improvement-round4.md`（S12 / S13。結果は本書 §0）

本書は「次に何を、どの順で、どのフォルダで、どんなプロンプトで投げるか」だけを扱う運用文書。

---

## 0. ラウンド4の結果（2026-08-15 完了・main へマージ済み）

| セッション | 項目 | 結果 |
|---|---|---|
| S12 | R1 銘柄別SSRルート | 完了。`/t/[ticker]` を98銘柄で公開。sitemap 11 → **109 URL** |
| S13 | FU14 / FU16 / FU17 | 完了。3件とも決着。**FU17 は「銘柄種別」でなく「データの性質」で判定** |

マージ後の main で `npm test`（**63/63**）・`npm run lint`（**0エラー / 272警告**、増減なし）・
`npm run build` の通過を確認済み。dev サーバーで `/sitemap.xml`・`/t/7203`・
`/api/stock?ticker=0331418A` の3経路を実測確認した。

### 実測した内容

| 確認 | 結果 |
|---|---|
| sitemap の銘柄URL数 | **98件**（`/t/…`）。既存11件と合わせて109 |
| `/t/7203` の HTTP / 初回応答 | 200 / **3.35秒**（Runtime Cache MISS 時） |
| SSR HTML 内の実測数値 | 期間リターン **+10.95%** / 年率ボラ **30.65%** / 最大DD **−30.88%** |
| メタデータ | `<title>トヨタ自動車（7203）株価分析</title>`・description・`canonical https://kabugenron.com/t/7203` |
| 免責 | ルートレイアウトの `Disclaimer` が銘柄ページにも出る（本文中に別途の注意書きも追加済み） |
| 投信の取得 | `0331418A` 200 / 21点。**全バーで OHLC 同値・volume=0 を確認**（FU17 の判定条件と一致） |
| FU17 の台帳 | 98ID すべてが実在パネルIDと一致（`page.tsx` 内の `id:` 251件と突合） |

### ラウンド4で新たに分かったこと — 次のラウンドの判断が変わる

> **①②③ は 2026-08-17 に対処済み**（コミット `cf09c48` / `7126c26` / `6378813`）。
> 以下は「何が起きていたか」の記録として残す。**現在の状態は各項の末尾を読むこと。**

**① `/t/[ticker]` は SSG されていない**

`export const dynamic = "force-dynamic"` のため、ビルド出力は `ƒ (Dynamic)` である。
R1 の受け入れ条件5「ビルド時間の実測値を報告する」は**満たす対象が消えた**（ビルド時間への影響ゼロ）。

さらに調べて、遅さの本体が別にあることが分かった。
**銘柄ページだけが `getStockData(symbol, "1y")` を呼んでいた。**
アプリ本体・`/api/stock`・全ベンチマーク取得は例外なく `range=10y` で、
`cacheKey()` は range を含む（`stock-data.server.ts:51`）。
つまり **`1y` は誰も温めない専用エントリ**で、常に MISS を踏みやすい構造だった。

**もう一つ、CDN では解決しない。** Vercel は検証済みSEOクローラに対して
CDN キャッシュを **BYPASS** する（`cacheReason: crawler`）。
クローラの初回応答を支えられるのは Runtime Cache か ISR だけである。

**対処（`cf09c48` / `7126c26`）**:
- 取得を `10y` に寄せ、掲載する直近1年は `buildTickerPageSummary()` が切り出す。
  **通常のアプリ利用が銘柄ページのキャッシュを温めるようになった**
- `next.config.ts` に `/t/:ticker` の `Vercel-CDN-Cache-Control`
  （`s-maxage=900, stale-while-revalidate=86400`）。**これはクローラには効かない**。
  効くのは人間の再訪・SNS展開・プレビュー取得
- ISR 化はしていない（`cache: "no-store"` が動的に倒すため、価格取得層に手を入れる話になる）。
  `generateStaticParams()` は将来の起点として残してある

→ **S14 への申し送り**: OG画像のキャッシュ設計を CDN 前提で組まないこと。
`s-maxage=900` は `/t/:ticker` の1セグメントにしか当たらないので、
`/t/:ticker/opengraph-image` は自分で方針を決めること。

**② `/t/7203.T` は 404 になる（リダイレクトしない）**

`dynamicParams = false` は、`generateStaticParams()` に無い params を
**ページ本体の実行前に** 404 にする。そのため `page.tsx` に書かれた
Yahoo記法 → canonical の `permanentRedirect` は**一度も実行されない死んだコード**である。

**対処（`cf09c48`）**: `dynamicParams = true` に戻した。実測で
`/t/7203.T` → **308 → `/t/7203`**、`/t/7203.t` も同じ。未知のコードは
`getTickerPageInstrument()` → `notFound()` が受けるので防御は変わらない。

補足: `/t/aapl` は 308 にならず 404 のままだが、これは正しい。
**AAPL は公開対象ではない**（`TICKER_PAGE_INSTRUMENTS` は `major100` 由来＝国内株のみ）。
大文字化の正規化そのものは `7203.t` で効いていることを確認した。

**③ 非対応パネルの台帳は fail-open である**

FU17 の 74＋24 = 98 IDは `app/page.tsx` にハードコードされている。
**新しいパネルを追加して台帳に載せ忘れると、投信で無意味な結果がそのまま出る。**
壊れる方向ではないので気づきにくい。

**対処（`6378813`）**: 台帳を `app/lib/panel-data-requirements.ts` へ出し、
3つ目の `CLOSE_ONLY_SAFE_PANEL_IDS`（153件）を置いた。
`page-wiring.test.ts` が **`page.tsx` の全251IDがちょうど1分類に属すること**を検査するので、
分類を忘れたパネルは `npm test` が落とす。逆向き（改名で台帳が腐る）も同時に検査する。

**残る2つの穴（テストでは埋まらない）**:
- `SAFE` は**検証済みという意味ではない**。移設時点の挙動を写しただけである。
  実際、高値・安値を必要とするのに SAFE に入っているものが4件見つかった
  （`tech-adx` / `tech-breakout` / `vol-atr` / `tech-stoch`）。
  挙動を変えないため今回は SAFE のまま残し、理由をファイル冒頭に記録した → **FU26**
- `discretionary`（裁量トレード）節は `AccordionSection` を使わず
  `DiscretionaryLab` を直接描くため、**パネルIDを持たずこの仕組みの外にある** → **FU27**

**④ P2（段階取得）は「見送り」で確定した**

ラウンド3の brotli 実測（実転送 47.5 KB）を根拠に、親文書 §11 の状態を
**見送り判断待ち → 見送り**へ更新した。復活条件は FU10 と同じ
（転送量が問題であるという実測証拠が出たとき）。**以後 Codex は再検討しないこと。**

---

## 0.5 第1波（S14 / S16）の結果 — 2026-08-21 完了・main へマージ済み

| セッション | 項目 | 結果 |
|---|---|---|
| S14 | R3b 銘柄別OG画像 ＋ FU21 | 完了。98銘柄で数値入りカードを生成。**FU21 は force-dynamic 維持で決着** |
| S16 | FU11 / FU12 / A4 | 完了。**Canvas の低コントラスト灰色は残り0件。** A4 は米国式維持で決着 |

マージ後の main で `npm test`（**83/83**）・`npm run lint`（**0エラー / 272警告**、増減なし）・
`npm run build` の通過を確認済み。`npm start` の本番ビルドで OG画像4枚を実測取得した。

### 実測した内容（S14）

| 確認 | 結果 |
|---|---|
| `/t/7203/opengraph-image` | 200・PNG 97 KB・**初回1.99秒 / 2回目0.10秒**（Runtime Cache の HIT 差） |
| 日本語の描画 | **豆腐なし。**「トヨタ自動車」「年率ボラティリティ」「2026年8月20日時点」すべて正常 |
| 長い銘柄名 | `9983` ファーストリテイリング（11字）が `nameFontSize()` で自動縮小し**あふれなし** |
| フォールバック | `9613`（`PRICE_UNAVAILABLE_TICKERS`）→ 数値も銘柄名も無い汎用カードで **200**（500にしない） |
| フォント被覆 | 98銘柄の `name`/`ticker`/`market`/`currency` と固定文言を照合し**未収録0字** |
| キャッシュヘッダ | `public, max-age=0, s-maxage=28800, stale-while-revalidate=604800` |
| メタタグ | `og:image` / `twitter:image` に銘柄別URL、`twitter:card` が `summary_large_image` |
| 制約の遵守 | ドメイン `kabugenron.com` を焼き込み済み。「買い」「売り」と読める文言なし |

### 実測した内容（S16）

| 確認 | 結果 |
|---|---|
| Canvas の低コントラスト灰色 | `#9ca3af`/`#94a3b8` **残り0件**（コメント内の言及1件のみ） |
| `text-gray-400` | **残り0件**（46件を本文20/装飾26に振り分けて是正） |
| `text-green-600` | **残り0件**（272件を green-700 へ。暗色パネルの400系は据え置き） |
| ローソク足の第2の手がかり | 画面で確認。**上昇＝中空（白抜き）／下落＝塗りつぶし**＋直下に凡例文 |
| 方向記号 | ベンチマーク比較表に `▲0.19%` `▲226.09%` が緑700で描画。`aria-label` に「上昇」 |
| チャート描画方式 | `createChart` / `getContext("2d")` の差分**ゼロ**。CLAUDE.md のチャート規約は不変 |

### 第1波で新たに分かったこと — 次のセッションの判断が変わる

**① 両ブランチとも 2026-08-17 の3コミットより前の main から分岐していた**

worktree を作ったのが `90cdd7a` 時点で、その後 main に
`cf09c48` / `7126c26` / `6378813` が入った。**git のマージは3件とも無衝突だった**が、
無衝突であることは意味の整合を保証しない。実際、
**`opengraph-image.tsx` が `getStockData(symbol, "1y")` のまま残っていた。**

`cf09c48` が銘柄ページを `10y` へ寄せたのは、`cacheKey()` が range を含むため
`1y` が「アプリ本体も銘柄ページも温めない専用エントリ」になるからである（§0①）。
OG画像だけが `1y` を踏み続けると、**画像生成のたびに MISS を引く。**
マージ後に `10y` へ揃えた（`bdedcd9`）。掲載する直近1年は `buildTickerPageSummary()` が
切り出すので数値は変わらない。

> **教訓: 長寿命の worktree は、投入前に `git merge main` で main を取り込ませること。**
> 無衝突マージを「整合が取れている」と読まないこと。次の S15 / S17-S19 では
> **ブランチを切る直前に main を最新化する**か、着手時に main を取り込むこと。

**② `next/og` のフォント同梱は base64 埋め込みが正解だった**

Next のドキュメントにある `fetch(new URL("./x.ttf", import.meta.url))` は
Turbopack ビルドで相対URLに解決され、Node の fetch が落ちる（S14 が実測）。
`fs.readFile` はファイルトレーシング頼みで、落ちたときに**画像だけが静かに退化する**。
`_fonts/subset-base64.ts` に base64 で埋め込む形はどちらにも依存しない。
**S18 で画像を扱うなら同じ手を使うこと。**

**③ 「数えた対象がそもそも不完全」という失敗の形が再発した**

FU12 は `#9ca3af` / `#94a3b8` の2値で386箇所と数えていたが、同じ Canvas 内に
`#999`(46) `#888`(8) `#aaa`(4) `#bbb`(3) `#ccc`(3) `#a3a3a3`(1) の短縮表記が別にあった。
2値だけ直すと**受け入れ条件「軸ラベルのコントラストが改善している」を満たしたまま欠陥が残る**
（FU18 と同じ形）。S16 は色の値ではなく**用途**で網を張り直して塞いだ。

> **教訓: 受け入れ条件に「N箇所」と書いたら、そのNの数え方自体を疑うこと。**
> 「grep の網に入らない表記が別にないか」を各セッションの最初に確認させること。

**④ A4 の配色方針は「米国式維持」で決着した（§8 V7・V8 は決定済みに更新すること）**

理由は `app/lib/chart-colors.ts` の `DIRECTION_COLORS` に記録済み。要点は
**赤が二重の意味になる**こと（本アプリの `#dc2626` は204箇所で「有意・警告・悪い」を担う）。
切替設定は採らなかった（up/down と別の意味の赤・青を機械的に選別できないため）。
**以後この論点を再検討しないこと。** 投資先は配色ではなく第2の手がかりである。

**⑤ A4 は「全部入った」わけではない — 残りは S18 の入口**

`DirectionValue` が入ったのは**利用者が最初に触れる4節（基本・テクニカル・OHLC・リスク）の
表37箇所**である。以下は色だけが手がかりのまま残っている:

- **`app/page.tsx` 冒頭のサマリーカード「期間変動」**（画面で最も目立つ数値。S15 の専有ファイル）
- 残り14節の表・凡例
- Canvas 内で系列を色だけで区別している箇所（凡例に形・線種が無い）

**S18（A3 = Canvas 代替テキスト）と同じ領域なので、S18 に引き継ぐこと。**

**⑥ `chart-colors.ts` は `globals.css` と値を二重に持っている**

Canvas は CSS 変数を読めないため、`@theme inline` のトークンと同じ値を複製している。
**片方だけ変えると乖離する。** ファイル冒頭に対応表があるが、
**テストで縛られてはいない。** M3（S19）のレジストリ化で拾う候補。

---

## 0.6 第2波（S15）の結果 — 2026-08-23 完了・main へマージ済み（`fe392ab`）

| セッション | 項目 | 結果 |
|---|---|---|
| S15 | U1 パネル結果バッジ | 完了。**方針 (a)「開封済みパネルの要約だけを保持」を採用。** 事前計算は入れていない |

マージ後の main で `npm test`（**83/83**）・`npm run lint`（**0エラー / 272警告**、増減なし）・
`npm run build` の通過を確認済み。

### 何がどう変わったか

**① 矛盾は「解かず」に、片側を捨てて決着した**

プロンプトが提示した矛盾（「開かずに結果を出す」×「開くまで計算しない」）に対し、
S15 は **(a) を採り、(b) 軽量判定の新設と (c) サーバー事前計算を採らなかった。**
結果として **「まだ一度も開いていないパネルにはバッジが出ない」** という制限が残る。
これは §9 の「全パネル事前計算の禁止」を守った代償であり、**仕様どおりである。**

**② 実装は S13 の `AnalysisAvailabilityContext` に相乗りした（新機構は作っていない）**

- `AnalysisAvailabilityProvider` に `resultSummaries` / `reportResultSummary` を追加
- パネル本体が `useAnalysisResultSummary(パネルID, 要約)` で**既に計算済みの値**を Provider に預ける
- `CollapsibleAnalysis` は `summary` prop を受けて見出し右端にピル型バッジを描く
- **本体がマウントされるまでフックは呼ばれない** → 閉じたままのパネルは1回も計算しない

**③ バッジの表示規約（S18 / S19 はこれに合わせること）**

- 色は `chart-colors.ts` の **`DIRECTION_TEXT_CLASS`**（新色を作っていない）
- **記号 `DIRECTION_GLYPH`（▲▼→）と `aria-label` を必ず併記**（A4 の第2の手がかり）
- 所見が無いときも `→ 所見なし` を出す（「未計算」と「所見なし」を区別する）

**④ 非対応パネルとの優先順位は「非対応が勝つ」**

`AccordionSection` は `unavailableItemIds` に載るパネルには **`summary` を渡さない。**
本体もマウントされないので、そもそも要約が生成されない。
Pro ロック（`LockedPanel`）も同様にバッジ無し。**受け入れ条件4は構造的に満たされている。**

**⑤ バッジの寿命は `summaryScope`（銘柄＋期間＋最終足）に紐づく**

`app/page.tsx` が `${ticker}:${period}:${最終足のtime}:${最終足のclose}` を Provider に渡し、
**これが変わると保持していた要約を全部捨てる。** 古い銘柄のバッジが次の銘柄に残らない。
localStorage には保存していないので、**リロードでバッジは消える**（開閉状態だけが復元される）。

**⑥ 実装したのはテクニカル節の7パネル**

`sa-technical` / `tech-adx` / `tech-stoch` / `tech-obvvwap` / `tech-extra` /
`tech-breakout` / `tech-vw`。**残り約304パネルはバッジ無しで従来どおり動く**（後方互換）。

**⑦ ついでに A4 の残件を1つ潰した（FU30 の一部）**

`app/page.tsx` のサマリーカード「期間変動」が `DirectionValue` になった。
`SummaryCard` の `color` prop は廃止され、`value` が `ReactNode` を取るようになった。
**画面で最も目立つ数値が色だけの手がかりではなくなった。**

### 第2波で新たに分かったこと — 次のセッションの判断が変わる

**① 受け入れ条件3（初期表示時間の非悪化）は、測るまでもなく構造で保証されている**

数値比較の報告は無かったが、**バッジ経路には初期表示前に走るコードが1行も無い。**
`CollapsibleAnalysis` は閉じている間 `children` を描画せず、要約はパネル本体の
`useMemo` の**戻り値を読むだけ**である。バッジ用の計算は新設されていない。
**「測って同じだった」より強い保証なので、これで受け入れる。**

> **教訓: 受け入れ条件に「数値で報告」と書いても、設計が変われば
> その測定が意味を失うことがある。** 次からは「悪化しないこと」ではなく
> **「初期表示前に走る新規コードが無いこと」** を条件に書くほうが検証しやすい。

**② パネルIDがコンポーネント側にハードコードされた（新規 FU33）**

`useAnalysisResultSummary("tech-adx", …)` のように、**`app/page.tsx` が持つべきIDを
コンポーネントが文字列で複製している。** 今は7箇所だが:

- `app/page.tsx` でIDを変えるとバッジが**無言で出なくなる**（テストが無い）
- **S19（M3 レジストリ化）はまさにIDの所在を動かす作業である**

→ **S19 の着手前に、リテラルIDと `page.tsx` の台帳を突き合わせるテストを足すこと。**
`app/lib/__tests__/page-wiring.test.ts` が既に `page.tsx` をソースとして読んでいるので、
そこに `useAnalysisResultSummary(` の第1引数を集める検査を追加するのが最短である。

**③ FU26 の4件のうち3件に、S15 がバッジを付けた**

`tech-adx` / `tech-breakout` / `tech-stoch` は **FU26 で UNAVAILABLE に倒す候補**である。
現状の main では投信でもこの3つが SAFE 扱いなので、
**ADX 13.64 という偽の数値から作られたバッジが、閉じた見出しにまで出る。**
S17 が UNAVAILABLE に倒せば `summary` が渡らなくなり自動的に消えるので、**追加作業は不要。**
ただし **S17 は「バッジも一緒に消えること」を受け入れ条件に入れること。**

**④ バッジの「所見」の粒度がパネルごとに揃っていない**

`tech-breakout` は `標本 231件`、`tech-vw` は `乖離 3件` と、
**所見ではなく「計算できた量」をバッジにしている。** 一方 `tech-stoch` は `売られすぎ` と
判断を出す。**利用者にとっては同じ見た目のバッジである。**
横展開する前に「バッジは判断を出すのか、量を出すのか」を決めること。
**決めるのは S19 のレジストリ設計と同時が自然**（要約の型を台帳側に持たせられる）。

**⑤ Context 経由の要約は、パネル数を増やすと再描画が増える**

要約が1件届くたびに Context の値が変わり、`AccordionSection` が再描画される。
`it.node` の要素参照は不変なのでパネル本体は再描画されないが、
**`CollapsibleAnalysis` は全件が浅く再描画される。** 7パネルでは問題にならない。
**300パネルへ横展開するなら、要約の配布を per-panel の購読に分けること。**

---

## 0.7 第3波（S17 / S18）の結果 — 2026-08-28 完了・main へマージ済み（`b8a571e` / `c3c568f`）

| セッション | 項目 | 結果 |
|---|---|---|
| S17 | U2 / FU20 / FU23 / FU26 / FU27 | 完了。**5件とも決着。** 受け入れ条件はレビュー側の実機確認で7件中6件○・1件△ |
| S18 | A3 ＋ A4 の残り | **部分完了。** A3 は Canvas 150件のうち **10件**。A4 は表に184箇所。**「A3 完了」ではない** |

マージ後の main で `npm test`（**83/83**）・`npm run lint`（**0エラー / 272警告**、増減なし）・
`npm run build` の通過を確認済み。`npm start` の本番ビルドで
投信 `0331418A` の FU26 と `freq-wavelet` の代替テキストを実測した。

### 何がどう変わったか

**① FU26 は4件とも UNAVAILABLE に倒れた（CAUTION ではない）**

`tech-adx` / `tech-breakout` / `tech-stoch` / `vol-atr` を
`CLOSE_ONLY_SAFE_PANEL_IDS` から `CLOSE_ONLY_UNAVAILABLE_PANEL_IDS` へ移した。
本番ビルドで `0331418A` を開き、**4件とも「結果を表示しません」に変わり、
§0.6③ が予告したとおり S15 のバッジも自動的に消えた**ことを確認した（追加コードは不要だった）。
株式 `6758.T` では `tech-adx` に `▲強いトレンド` が出るので退行はない。

**② U2 は「一括開放だけを一時状態にする」で決着した**

個別の開閉は従来どおり `sa:open:<id>` で銘柄をまたいで永続する。
**「すべて開く」だけが localStorage に書かなくなった**（「すべて閉じる」は `0` を書く）。
実機で確認: `すべて開く` の後も `sa:open:*` は個別に開いたぶんしか増えず、
期間を変えると一括開放したパネルだけ閉じ、個別に開いたパネルは開いたまま残る。

**③ FU23 の catch は「上流障害だけ」に絞られた**

`shouldFallbackFromFundBff()` が `StockSourceError` かつ
401/403/408/425/429/5xx のときだけ RSC へ退避する。
**404 と実装例外（`StockSourceError` でないもの）は再送出**する。
網羅漏れを塞ぐため、fetch の network エラーと JSON パース失敗を 502 に包み直してある。
FU14 の本命経路（JWT が取れない → RSC）は `try` の外にあるので影響を受けない。
`0331418A` / `03311187`（`&` を含む名前）/ `9C31108A` の3本 × 1y/10y の6通りすべて
200 で 243〜2441点を返すことを実測した。

**④ FU20 は「取得を増やさない」で決着し、FU27 は「IDを付けない」で決着した**

sitemap は **コード変更ゼロ**（コメントのみ）。外部取得は1本も増えていない。
`discretionary` 節は「複数の入力・検証工程とシナリオ保存を一体で扱う常時表示ワークスペースで、
単一の折りたたみ分析ではない」ため擬似パネルIDを作らない、と台帳に明記された。
**S19 は「AccordionSection 外の節エントリ」を表せる種別を用意すること。**

**⑤ A3 は `AccessibleCanvas` という薄いラッパで実装された**

`description` を必須 prop にし、`role="img"` + `aria-label` を canvas 自身に付ける。
説明文は各コンポーネントの `useMemo` が**計算結果から生成**している（固定文言ではない）。
本番ビルドの実測例:

> `ウェーブレットスカログラム。直近2026-08-28では252.0日周期のパワーが最大で、全期間最大の57%です。`

`DirectionGlyph` も `role="img"` へ寄せ直され、**FU31 は解消**した。

**⑥ 描画方式は1件も変わっていない**

`createChart(` 142件・`getContext("2d")` 173件が main と完全一致。
CLAUDE.md のチャート規約は S18 の全面改修を通しても不変である。

### 第3波で新たに分かったこと — 次のセッションの判断が変わる

**① 「N箇所直した」の N を数えていないと、受け入れ条件そのものが検算できない**

S18 の受け入れ条件2は「Canvas を持つコンポーネントの棚卸し表がある」だった。
**その表が作られなかったので、条件1（A4 の残件を全件分類）も検算できなくなった。**
レビュー側で数え直した結果:

| | 件数 |
|---|---|
| `getContext("2d")` を持つコンポーネント | **150** |
| うち `AccessibleCanvas` を使うもの | **10** |
| 素の `<canvas>` が残るファイル | **173** |

条件3（代表的な分析で読み上げに実測値が乗る）は満たされている。
**つまり「条件を満たしたまま140件が手つかず」という FU18 とまったく同じ形である。**
入っている10件の質は高い（実測値から生成・二重読み上げ無し）ので差し戻しはしていないが、
**§1 の表では「A3 着手」であって「A3 完了」ではない。** 残りは **S20**。

> **教訓: 棚卸しを求める受け入れ条件は、他の条件より先に満たさせること。**
> 母数が出ていないと、後続の条件が「満たした」に見えても検算できない。
> S20 のプロンプトでは **棚卸し表の提出を第1コミットの条件にする。**

**② 記号は色より粒度が細かい。`eps` を渡さないと両者がずれる**

S18 は `DirectionGlyph` を184箇所に足したが、**`eps` を指定した箇所は0件**である。
一方、既存の色付けには不感帯がある箇所が多い:

| 箇所 | 色の閾値 | 記号の閾値 | 起きること |
|---|---|---|---|
| `PersistenceChart` の相関 | `>0.5` 緑 / `<0` 赤 / それ以外 灰 | `0` | 相関0.3 で「▲」と「（再現性は限定的）」が同居 |
| 勝率（複数箇所） | `>0.52` / `<0.48` | `0.5` | 0.505 で色は灰・記号は▲ |
| IC | `\|ic\|>=0.1` | `0` | ic=0.02 で色は無彩・記号は▲ |

**色と記号は同じ判定から出さないと、第2の手がかりが第1の手がかりと矛盾する。**
A4 の趣旨（色が見えなくても同じ結論に達する）を壊す向きなので、S20 で `eps` を通すこと → **FU36**。

さらに**方向を持たない量にも記号が付いた**。最大ドローダウンは定義上つねに ≤0 なので
**▼が常時点灯する**（`VolTargetingChart` ほか3箇所）。
§0.5 と S18 のプロンプトが「記号が常に片側に倒れる」として警告していた形そのもの → **FU37**。

> **教訓: A4 の作業は「符号のある量か」を1件ずつ判断する必要があり、機械的に広げられない。**
> S16 が37箇所で止めたのは慎重すぎたのではなく、**これが1件ずつの判断だからである。**

**③ 専有ファイルの線は、目的を達成するのに足りていなかった（S17 が越えた）**

S17 は `AccordionSection.tsx` を編集した。**両セッションに対して明示的に禁止した唯一のファイルである。**
理由は正当で、**U2 は `CollapsibleAnalysis` の永続層だけでは実装できなかった**:
「一括開放を現在の銘柄・期間の内側だけの一時状態にする」には、
S15 の `summaryScope` を Context から取り出して React key に混ぜる必要があった。

```
key={`${it.id}:${availability.summaryScope ?? "unscoped"}`}
```

S18 は同ファイルを開いていないので**衝突は起きなかった**が、線の引き方の問題が残る。

> **教訓: 「触ってはいけないファイル」を決めるときは、
> そのセッションの担当項目がそのファイル無しで実装できるかを先に確かめること。**
> U2 は「`CollapsibleAnalysis` の永続層」と書かれていたが、実際には親の再マウント制御が要った。

**④ その key は全パネルの再マウントを意味する（S19 で必ず踏む）**

`summaryScope` は `${ticker}:${period}:${最終足のtime}:${最終足のclose}` である。
つまり**銘柄・期間・最終足のどれかが変わるたびに、開いている全パネルが再マウントされる**
（従来は再描画のみ）。実際、初回ロード直後に「すべて開く」を押した直後に
**全パネルが閉じる現象を1回再現した**（データが後から確定して scope が変わったため）。
落ち着いてから押せば安定するので、再現条件は握れていない。

なお **「期間を変えるとパネル内のサブタブが RSI に戻る」現象は main でも同じ**だった
（S17 の退行ではない。パネル本体が prices の変化で状態を捨てている）。

**S19 は `AccordionSection` の構造を組み替えるので、この key と
FU35（要約の per-panel 購読）を同時に見直すこと。** → **FU41**

**⑤ 「テストで縛った」という主張は、そのテストが今回足されたのかを見ること**

S17 は FU20 の判断根拠として「`ticker-pages.test.ts` が既知の除外と台帳の空洞化を固定する」と書いた。
**そのテストは main に元からあり、S17 は1行も足していない。**
実際に固定しているのは `9613` が除外されていることと `TICKER_PAGE_INSTRUMENTS.length > 50` の2点で、
**新たに取得不能になった銘柄は検知しない。** 判断（外部取得を増やさない）は妥当だが、
FU20 の本体である「手動台帳が腐る」は未解決のまま → **FU40**。

> **教訓: 「既存のテストが守っている」と書かれていたら、
> そのテストを開いて何をアサートしているかまで読むこと。** ファイル名は根拠ではない。

**⑥ FU23 の narrowing で挙動が1つだけ変わった（本番で見る価値がある）**

BFF が空配列を返したときの `No price data`（404）が、これまでは RSC 経路へ退避していたのが
**そのまま 404 になる。** 手元の3本では再現しないが、**古い投信・償還間近の投信で
出る可能性がある。** §5 の本番確認リストに入れた。

**⑦ S18 が `chart-colors.ts` を使わずハードコード色を足した（FU29 の同型再発）**

凡例に線種を足した3ファイルで `#16a34a` / `#dc2626`（= `DIRECTION_COLORS.up/down` と同値）を
**リテラルで複製**している。`chart-colors.ts` は S18 の専有ファイルだったので import できたはずである。
FU12 が集約したものを少しずつ崩す向き → **FU39**。

---

## 1. ラウンド5で投げるもの

### 第1波 — **完了（2026-08-21）**

| # | セッション | 項目 | 結果 |
|---|---|---|---|
| ~~S14~~ | 銘柄別OG画像 | R3b ＋ FU21 | **マージ済**（`b07d684`）。追い修正1件（`bdedcd9`）→ §0.5① |
| ~~S16~~ | アクセシビリティのまとめ取り | FU11 + FU12 + A4 | **マージ済**（`339a8ba`） |

worktree `..\sa-s14` / `..\sa-s16` は削除済み。ブランチ `feat/ticker-og` /
`feat/a11y-canvas` も削除済み。**フォルダの残骸が無いことを確認済み。**

> S16 は「`app/page.tsx` を触らない」線を1行だけ越えた（`text-green-600` →
> `text-green-700` の一括是正。§0.5 の色の網に含まれる1箇所）。
> **S15 の着手時点で main に入っているので衝突しない**が、S15 には
> 「サマリーカードの色は既に green-700 になっている」と伝えること。

### 第2波 — **完了（2026-08-23）**

| # | 項目 | 結果 |
|---|---|---|
| ~~S15~~ | **U1** パネル結果バッジ | **マージ済**（`fe392ab`）。方針 (a)。テクニカル節7パネル＋ FU30 の1件 → §0.6 |

worktree `..\sa-s15` は削除済み。ブランチ `feat/panel-badge` も削除済み。

> S15 は `AnalysisAvailabilityContext` を**拡張して**相乗りした（新機構なし）。
> **S17 / S18 / S19 はこの Context を壊さないこと。**
> バッジの表示規約（色・記号・aria-label・非対応優先）は §0.6③④ にある。

### 第3波 — **完了（2026-08-28）**

| # | 項目 | 結果 |
|---|---|---|
| ~~S17~~ | **U2** ＋ **FU20 / FU23 / FU26 / FU27** | **マージ済**（`b8a571e`）。5件とも決着 → §0.7 |
| ~~S18~~ | **A3**（Canvas 代替テキスト）＋ **A4 の残り** | **マージ済**（`c3c568f`）。**A3 は 150件中10件＝着手にとどまる。** 残りは S20 → §0.7① |

worktree `..\sa-s17` / `..\sa-s18` は削除済み。ブランチ `feat/panel-followups` /
`feat/canvas-alt-text` も削除済み。**フォルダの残骸が無いことを確認済み。**

> S17 は `AccordionSection.tsx` を触った（**両者に禁止した唯一のファイル**）。
> U2 が親の再マウント制御を必要としたためで、理由は正当だが線の引き方に穴があった → §0.7③。
> **S19 はこの key（`${it.id}:${summaryScope}`）を FU35 と一緒に見直すこと** → §0.7④。

### 第4波 — **次はここ。S19 は単独、S20 は S19 の後**

| # | 項目 | 専有ファイル | 規模 |
|---|---|---|---|
| S19 | **M3** レジストリ化（`panel-sections.ts` と `panel-data-requirements.ts` を吸収する） | `app/page.tsx`, `app/lib/panel-*.ts`, `AccordionSection.tsx` | 大 |
| S20 | **A3 の残り**（Canvas 150件中140件）＋ **FU36 / FU37**（記号と色のずれ） | Canvas 描画コンポーネント群, `app/lib/chart-colors.ts` | 大 |

**S19 は必ず単独。** `app/page.tsx` の構造を組み替えるので他と並べられない。
**S20 は S19 の後に単独で投げる。** S18 と同じ領域を大規模に触るので、
レジストリが固まってからのほうが差分が読みやすい。

### 明示的に見送るもの

- **P2 段階取得** — §0④ のとおり確定。再検討しない
- **FU10 価格の有効桁** — 8桁で確定済み。再検討しない

---

## 2. worktree の準備

> ### ⚠ シェルを間違えないこと
> **この環境の既定ターミナルは PowerShell である。** bash 版を PowerShell に貼ると
> `$dir` / `$br` が展開されず、リポジトリ内部に不正な worktree が作られる（2026-08-11 に発生）。
> 誤作成したら: `git worktree remove '$dir' --force; git worktree prune; git branch -D '$br'`

### PowerShell 版（この環境の既定シェル）

```powershell
Set-Location C:\Users\hikar\next\stock-analysis

if (git status --porcelain) {
  Write-Host "中断: 未コミットの変更があります。worktree から見えないので先にコミットしてください。"
  git status --short
} else {
  # ★ 第1波（S14/S16）・第2波（S15）・第3波（S17/S18）は完了・削除済み。
  # 次に作るのはここ。**S19 は単独。並列にしないこと**:
  #   S19 の後 : @{ Dir = '..\sa-s20'; Branch = 'feat/canvas-alt-text-rest' }  ← これも単独
  $pairs = @(
    @{ Dir = '..\sa-s19'; Branch = 'feat/panel-registry' }
  )
  foreach ($p in $pairs) {
    git worktree add $p.Dir -b $p.Branch
    if ($?) { Push-Location $p.Dir; npm install; Pop-Location }
  }

  Write-Host "`n完了。参照文書の存在を確認します:"
  $docs = @(
    'AGENTS.md', 'CLAUDE.md', 'NEXT_SESSION.md',
    'docs/site-improvement-execution-plan.md',
    'docs/site-improvement-round4.md', 'docs/site-improvement-round5.md'
  )
  foreach ($p in $pairs) {
    $n = ($docs | Where-Object { Test-Path (Join-Path $p.Dir $_) }).Count
    "  {0} : 6文書中 {1} 個" -f $p.Dir, $n
  }
}
```

**6/6 にならなければ投入しないこと。**

### ⚠ 分岐点を古くしないこと（§0.5① の教訓）

`git worktree add -b` は**その時点の HEAD から切る。** 第1波は worktree を作った後に
main へ3コミット入り、**無衝突マージなのに意味が壊れた**（OG画像だけが `range=1y` に
取り残された）。したがって:

1. **worktree はセッションを投げる直前に作ること。** 先に作り置きしない
2. セッションが数日にまたがったら、**作業ブランチで `git merge main` を実行させること**
3. マージ後は「無衝突だった」で終わらせず、
   **`main` 側の変更が触った概念を作業ブランチ側でも grep すること**
   （今回なら `getStockData(` の range 引数）

---

## 3. 各セッションのプロンプト

### 投げ方（これだけやればよい）

プロンプトは「共通ひな形＋各セッション」の2つに分かれている。**手で合成しないこと。**
ひな形を直したのに片方へ反映されず、古いまま投げる事故になる（§0.5① と同じ形）。
合成済みのファイルを作るスクリプトがある:

```powershell
Set-Location C:\Users\hikar\next\stock-analysis

# ① worktree を作る（投げる直前に実行すること。判定つきの版は §2 にある）
git worktree add ..\sa-s19 -b feat/panel-registry
Push-Location ..\sa-s19; npm install; Pop-Location

# ② 合成済みプロンプトを作る（docs/prompts/s19.md ができる）
node docs/tools/build-session-prompts.mjs S19

# ③ クリップボードへ入れて、そのセッションに貼る
Get-Content docs/prompts/s19.md -Raw | Set-Clipboard
#   → S19 のセッションを ..\sa-s19 で開いて貼る
```

**引数を省くと未完了のセッション全部を書き出す。**
完了済みは見出しが `### ~~S15~~ —` の形になるので自動的に外れる。
**マージして完了にしたら、見出しを `~~` で囲むこと**（次から生成対象から落ちる）。

ひな形や各プロンプトを直したら、**②を流し直してから貼ること。**
`docs/prompts/` は**生成物である。** そこを直しても次の生成で消える。直すのは常に本書 §3。

---


### 共通ひな形（各プロンプトの冒頭に含める）

```
このリポジトリ（Next.js 16 App Router の株価分析サイト）の改善作業を担当してほしい。

## 最初に読む（順番厳守）
1. AGENTS.md
2. CLAUDE.md
3. docs/site-improvement-execution-plan.md の §0 / §1.3 / §2 / §6 / §7 / §9
4. docs/site-improvement-round5.md の §0 / §0.5 / §0.6 / §0.7
   （前のラウンドと第1・第2・第3波で分かったこと。**必読**。
   とくに §0.7 は直前の波なので、前提が変わっている）
5. docs/site-improvement-round5.md §6 のフォローアップ表（FU41 まである。こちらが最新）

§6（共通制約）と §9（やってはいけないこと）は必ず守ること。

**これらのファイルが見つからない場合は、作業を始めずに即座に報告すること。**
親リポジトリを探しに行ったり、記憶や推測で補ったりしないこと。

## テストがある
npm test で 83件の回帰テストが走る。**壊さないこと。**
価格・統計まわりを触ったら必ず実行すること。
テストが落ちたら、テストを直す前に「実装とテストのどちらが正しいか」を報告すること。

app/lib/__tests__/page-wiring.test.ts は app/page.tsx を**ソースとして読み**、
パネルID→節の対応と SERIES_AWARE_SECTIONS の宣言が実態と一致しているかを検査する。
パネルを追加・改名したらここが落ちる。落ちたらテストを緩めず、
表（app/lib/panel-sections.ts）と page.tsx のどちらが正しいかを報告すること。

## 目視確認は docs/visual-check-matrix.md に従う
「ブラウザ確認済み」とだけ書かないこと。**どの行を見て、どの行を見ていないか**を報告に書く。
FU18（破損点検パネルが全セクションの最上段を占有）は、受け入れ条件を満たしたまま
22節ぶん問題が残り、「確認済み」の中に埋もれていた事例である。

## ★ 「N件直した」と書く前に、その N の分母を先に出すこと
同じ失敗が3回繰り返されている（FU18・FU12の短縮表記・S18 の A3）。
**対象を数えて分類した表を先に出し、その後で実装に入ること。**
分母が無いと、受け入れ条件を満たしているかをレビュー側が検算できない。
grep の網に入らない表記が別に無いかを自分で引くこと。

## 専有ファイルの線を守ること
下の「専有ファイル」以外は読んでよいが、編集しないこと。
編集が必要だと判断したら、**勝手にやらずに止めて報告すること。**

**線を越えたなら、越えた事実を報告に必ず書くこと。**
S17 は禁止された `AccordionSection.tsx` を編集した（理由は正当だったが、
担当項目がそのファイル無しでは実装できなかった）→ §0.7③。
**「実装してみたら線の外が必要だった」なら、それ自体が報告すべき発見である。**

## 進捗表について
docs/site-improvement-execution-plan.md の §11 進捗表は**更新しないこと**。
マージ後に人間側でまとめて更新する。

## 進め方
- 実装方法は任せる。計画書は制約と受け入れ条件だけを定めている
- 計画書の記述と実際のコードが食い違ったら、コードを正として報告する
  （ラウンド3で2件、ラウンド4で3件見つかった。計画書は完全ではない）
- 迷う判断は勝手に決めず、選択肢と推奨を挙げて確認すること
- 完了したら npm test → npm run lint → npm run build → ブラウザ確認
- コミットは日本語・1コミット1目的（AGENTS.md 準拠）
- git add -A は使わない。パスを明示して add すること
- 最後に、変更の要約・未検証項目・判断が割れた箇所を報告すること
```

---

### ~~S14~~ — 銘柄別OG画像（R3b）＋ FU21 ※**完了 2026-08-21**（`b07d684`）。投げたプロンプトの記録

> **〜完了 2026-08-21〜** マージ済み（`b07d684`）。結果は §0.5。以下は投げたプロンプトの記録。


```
作業ディレクトリ: C:/Users/hikar/next/sa-s14
ブランチ: feat/ticker-og

［共通ひな形をここに貼る］

## 担当
docs/site-improvement-execution-plan.md の R3b（§3 グループR「R3. OG画像」の後半）。
**併せて FU21（/t/[ticker] のキャッシュ方針）を決着させること。** 順序は FU21 が先。

## 専有ファイル
- app/t/[ticker]/opengraph-image.tsx（新規）
- app/t/[ticker]/page.tsx
- app/lib/ticker-pages.ts
- フォント資産（配置場所は任せる）

**app/page.tsx・app/components/ 配下は触らないこと**（別セッションが編集中）。

## 前提（ラウンド4で完成済み）
- `/t/[ticker]` は **98銘柄で稼働中**。`app/lib/ticker-pages.ts` に
  `TICKER_PAGE_INSTRUMENTS` / `getTickerPageInstrument()` / `buildTickerPageSummary()` がある
- サマリーの計算（現在値・期間リターン・年率ボラ・最大DD）は
  **`buildTickerPageSummary()` を再利用すること。** OG画像用に別実装を作らない
- 静的OG画像（R3a）は完了済み。`twitter:card` は既に大カード

## まず FU21 を決めること（ここが本題の半分）
現状 `app/t/[ticker]/page.tsx` は `export const dynamic = "force-dynamic"` で、
ビルド出力は `ƒ (Dynamic)`。**SSG されていない。**
価格キャッシュは `getStockData` の Runtime Cache（fresh 8時間・保持7日）だけが担い、
**MISS 時の初回応答は実測 3.35秒**。

OG画像はここにさらに画像生成を重ねる。したがって:
1. `force-dynamic` を維持するのか、ISR（`revalidate`）へ寄せるのかを**先に決めて報告すること**
2. どちらを採るにせよ **OG画像のキャッシュ設計を PR 本文に書くこと**（受け入れ条件2）
3. 価格取得は必ず `getStockData` 経由（CLAUDE.md 最優先規約）。
   **OG画像から /api/stock を自己呼び出ししないこと**

## 日本語フォントが要る
`next/og` は CJK を内蔵しない。**フォントを同梱しないと日本語は豆腐になる。**
- サブセット化してファイルサイズを抑えること（R3a は 1,026KB → 135KB に落とした前例がある。FU5）
- ライセンスを確認し、出典をコメントに残すこと

## 制約
1. **画像内にドメイン名（kabugenron.com）を焼き込むこと**（growth-strategy.md B-2）
2. **「買い」「売り」と読める文言を画像に入れないこと**（app/lib/tiers.ts 冒頭の設計方針）。
   数値と銘柄名の提示にとどめる
3. 価格取得に失敗した銘柄では、**数値の入らない汎用画像へフォールバックすること**
   （現行ページは取得失敗時に `noindex, follow` を返す。画像側で500にしない）

## 受け入れ条件
1. `/t/[ticker]` ごとに銘柄名と実測数値を含む画像が生成される
2. 日本語が正しく描画される（豆腐にならない）ことをスクリーンショットで確認
3. 画像生成が本番のレスポンスを著しく遅らせない。**キャッシュ設計を報告に記載**
4. FU21 の判断（force-dynamic 維持 / ISR）と理由が記録されている
5. 取得失敗銘柄でフォールバック画像が返る
6. npm test / npm run lint / npm run build が通る

## 検証用の銘柄
- 株式: `7203`（トヨタ自動車。名前が長くない対照群）
- 長い銘柄名の銘柄を1つ選び、**文字あふれの処理**を確認すること
```

---

### ~~S16~~ — アクセシビリティのまとめ取り（FU11 / FU12 / A4）※**完了 2026-08-21**（`339a8ba`）。投げたプロンプトの記録

> **〜完了 2026-08-21〜** マージ済み（`339a8ba`）。結果は §0.5。以下は投げたプロンプトの記録。


```
作業ディレクトリ: C:/Users/hikar/next/sa-s16
ブランチ: feat/a11y-canvas

［共通ひな形をここに貼る］

## 担当
docs/site-improvement-execution-plan.md の FU11・FU12（§11 フォローアップ表）と A4（§3 グループA）。
**3件は対象領域が重なるため、1セッションでまとめて扱う。**

## 専有ファイル
- app/axioms/page.tsx（FU11 ②）
- Canvas 描画を持つ分析コンポーネント（FU12・A4）
- 配色定数を置く新規ファイル（作るなら app/lib/ 配下。場所は任せる）

**触らないこと:**
- **app/t/ 配下**（S14 が編集中）
- **app/page.tsx**（FU17 の非対応パネル台帳がある。S15 の専有でもある）
- app/lib/stock-data.server.ts / price-sanity.ts（回帰テスト対象）

## 3件の内容

### FU11: A1 の残 46箇所
A1（2026-08-11）で components 配下 190ファイル・868箇所を `text-fg-muted`(#4a5565) へ置換した。
残った46箇所の内訳は:
- ① 装飾アイコン・UIボタン（`CollapsibleAnalysis` の ▶ 等）。
  **非テキストは 4.5:1 ではなく 3:1 基準で判定すること**
- ② **`app/axioms/page.tsx` は A1 のスコープ外で手つかず**。本文テキストのAA未達が残存

### FU12: Canvas 内のハードコード灰色 386箇所
`#9ca3af` / `#94a3b8` が Canvas 描画コードに直接書かれている。
Tailwind の置換では届かない領域で、**チャートの軸ラベル等のコントラストは未改善**。

**386箇所を1つずつ書き換えないこと。** 共通の色定数へ寄せてから一括で変えること。
既存の `@theme inline`（Q3 のデザイントークン）と整合させること。

### A4: 色以外の符号化
色だけで情報を伝えている箇所（上昇/下降など）に、形・線種・ラベル等の第2の手がかりを足す。
**配色方針（米国式=上が緑 / 日本式=上が赤 / 切替可能）は未決定**（§8 V7・V8）。
**判断して理由を残すこと。放置しないことが目的である。**

## 進め方の指定
1. **まず影響範囲を数えて報告すること。** 386箇所の内訳（軸ラベル / グリッド線 /
   注釈 / 系列色）を分類し、どれがコントラスト対象でどれが装飾かを切り分ける
2. **配色方針（A4）を先に決めて確認を取ること。** これが決まらないと FU12 の
   置換先の色が決まらない
3. 置換は共通定数経由で一括。**チャートの見た目が変わるので、
   代表的な図を数枚スクリーンショットで前後比較して報告すること**

## 制約
- **CLAUDE.md のチャート規約を守ること。** 横軸が時間の系列は `lightweight-charts`、
  Canvas2D は横軸が時間でない静的図（分布・散布・QQ・相関行列・位相空間・
  ネットワーク等）のみ。**この作業でチャートの描画方式を変えないこと**
- `AnalysisGuide` 必須規約は維持
- パネルID は不変（共有URL・localStorage 互換）

## 受け入れ条件
1. FU11: 残46箇所の判定結果（是正 / 3:1基準で許容 / 対象外）が一覧で報告されている
2. FU11: `app/axioms/page.tsx` の本文テキストが AA を満たす
3. FU12: Canvas 内の灰色が共通定数経由になり、軸ラベルのコントラストが改善している
4. A4: 配色方針が決定され、理由が記録されている
5. 色以外の符号化が少なくとも代表的なチャート群に入っている
6. 前後のスクリーンショット比較がある
7. npm test / npm run lint / npm run build が通る
```

---

### ~~S15~~ — パネル結果バッジ（U1）※**完了 2026-08-23**（`fe392ab`）。以下は投げたプロンプトの記録

> **← 次はこれ。** 第1波はマージ済みなので、**いま投げられる。**


```
作業ディレクトリ: C:/Users/hikar/next/sa-s15
ブランチ: feat/panel-badge

［共通ひな形をここに貼る］

## 担当
docs/site-improvement-execution-plan.md の U1（§3 グループU）。

## 専有ファイル
- app/components/analysis/CollapsibleAnalysis.tsx
- app/components/analysis/AccordionSection.tsx
- app/page.tsx
- 要約を提供する側の lib / コンポーネント（試作する1セクション分のみ）

**このセッションは単独で走っている。** 上記は全節の共通土台なので、
他のセッションと並列にすると必ず衝突するため単独にした。

## 核心にある矛盾（ここを読まずに始めないこと）
**「開かずに結果を出す」と「開くまで計算しない」は原理的に衝突する。**
`CollapsibleAnalysis` が開くまで中身をマウントしないのは、
重い計算を止めるための**意図的な設計**である（同ファイルのコメントを読むこと）。

さらに P3 の調査で、10年・2,463点では `scanWeekdayEdges`（ブートストラップ込み）が
**229–269ms の Long Task** を再現することが分かっている（§8 V1）。
**全パネルを事前計算すると、この設計が防いでいる負荷が戻ってくる。**

## 取りうる方針（どれを採るかは任せる。ただし先に報告して確認を取ること）
- (a) 一度開いたパネルの要約だけを保持して次回以降バッジ表示（漸進的・低リスク）
- (b) 軽量な判定だけを別途計算する（要約専用の安価な関数を各分析が提供する）
- (c) サーバー側で事前計算する（R1 の実測サマリーと共通化できる可能性がある）

## ラウンド4で足場ができた（重要）
S13 が `AccordionSection.tsx` に **`AnalysisAvailabilityContext`** を入れた。
「パネルIDの集合を Provider で配り、各パネルの描画を切り替える」機構が既にある。
- 投信で解釈できない74パネルは本体をマウントせず理由を表示
- 24パネルには冒頭に注意書き

**U1 のバッジも同じ機構に相乗りできる可能性が高い。**
別の仕組みを新設する前に、この Context を拡張する案を検討して比較すること。

なお `app/page.tsx` には FU17 の非対応パネル台帳
（`CLOSE_ONLY_UNAVAILABLE_PANEL_IDS` 74件 / `CLOSE_ONLY_CAUTION_PANEL_IDS` 24件）がある。
**バッジは「非対応」と競合しうる**（非対応パネルにバッジを出しても意味がない）。
両者の優先順位を決めて実装すること。

## 制約
- **311コンポーネント全部を一度に対応させないこと。**
  未対応パネルはバッジ無しで従来通り動くこと（後方互換）
- パネルID は不変（共有URL・localStorage 互換）
- `AnalysisGuide` 必須規約は維持
- **全パネルの事前計算は禁止**（§9 の明示的禁止事項）

## 受け入れ条件
1. 少なくとも1セクション分のパネルで、閉じた状態から所見の有無が判別できる
2. 未対応パネルが従来通り動作する
3. **パネルを開く前の初期表示時間が悪化しない。F10 と比較して数値で報告すること**
4. 投信（`0331418A`）で、非対応パネルとバッジが競合していない
5. npm test / npm run lint / npm run build が通る
```

---

---

### ~~S15 への申し送り~~（第1波の結果を受けて追記したもの。**3件とも反映済み**）

```
## 第1波（S14 / S16）が main に入っている。前提が3つ変わった

1. **サマリーカードの色は既に AA 準拠である。** S16 が text-green-600 → green-700 を
   272箇所一括で是正し、app/page.tsx のサマリーカード1行もその対象に入った。
   **バッジの色を決めるときは app/lib/chart-colors.ts の
   DIRECTION_TEXT_CLASS / DIRECTION_TEXT_COLORS を使うこと。** 新しい色を作らない

2. **色以外の手がかりの共通部品が既にある。** app/components/analysis/DirectionValue.tsx
   （▲▼→ の記号＋符号＋aria-label）と DIRECTION_GLYPH。
   **バッジで「良い所見/悪い所見」を色分けするなら、必ず記号かラベルを併記すること**（A4）

3. **app/page.tsx 冒頭のサマリーカード「期間変動」は、まだ色だけが手がかりである。**
   §0.5⑤ のとおり A4 の残件で、**画面で最も目立つ数値**。S15 は app/page.tsx を
   専有するので、**ついでに DirectionValue を当てて構わない**（1箇所・低リスク）

## 分岐点を古くしないこと
worktree を作るなら**投げる直前に main から切ること**。数日またぐなら作業ブランチで
`git merge main` を実行すること。第1波は無衝突マージなのに意味が壊れた（§0.5①）
```

---

### ~~S17~~ — パネル周辺の後始末（U2 / FU20 / FU23 / FU26 / FU27）※**完了 2026-08-28**（`b8a571e`）。以下は投げたプロンプトの記録

```
作業ディレクトリ: C:/Users/hikar/next/sa-s17
ブランチ: feat/panel-followups

［共通ひな形をここに貼る］

## 担当
docs/site-improvement-execution-plan.md の U2（§3 グループU）と、
§11 フォローアップ表の FU20 / FU23 / FU26 / FU27。**5件は独立だが全部小さい。**

## 専有ファイル
- app/sitemap.ts（FU20）
- app/lib/panel-data-requirements.ts（FU26 / FU27）
- app/lib/stock-source.server.ts の catch（FU23）
- CollapsibleAnalysis の永続層（U2）

**触らないこと:**
- **Canvas 描画コード**（S18 の専有。配色定数 app/lib/chart-colors.ts も含む）
- **app/page.tsx の構造**（S19 が組み替える。台帳のIDを増減させるのは可）

## 5件の内容と、それぞれの罠

### U2: 「すべて開く」の永続範囲
現状は分析IDごとに localStorage へ開閉を保存し、**銘柄をまたいで維持される**
（memory: collapsible-analysis-pattern）。「すべて開く」を押した状態が
次の銘柄にも引き継がれてよいのかを決めること。**決めた理由をコードに残すこと。**

### FU20: sitemap が価格取得可否を検証しない
app/sitemap.ts は TICKER_PAGE_INSTRUMENTS を無条件に並べる。取得に失敗する銘柄は
ページ側が noindex を返すので、**sitemap に載せた URL を自分で noindex している**状態。
9613 は PRICE_UNAVAILABLE_TICKERS で手動除外しているが、**手動台帳は腐る**。

**罠: sitemap の生成時に98銘柄ぶん Yahoo を叩かせないこと。** ビルド/リクエストで
98本の外部取得を走らせる設計にすると FU15（投信10年が8秒）と同じ壁に当たる。
Runtime Cache に既にある銘柄だけを見る、生成を諦めて手動台帳をテストで縛る、など
**取得を増やさない案を優先し、選んだ理由を報告すること。**

### FU23: FU14 の退避 catch が広すぎる
投信の500を防ぐために入れた catch が、**本来知りたい失敗まで飲み込む**。
握りつぶす対象を「上流の特定の失敗」に絞り、それ以外は落とすこと。
**CLAUDE.md の最優先規約（価格は /api/stock 経由・repairPriceGlitches を通す）に
影響しないことを確認すること。**

### FU26: SAFE に高安を要するパネルが4件混ざっている
tech-adx / tech-breakout / vol-atr / tech-stoch。

**調査は 2026-08-23 に本番で済んでいる（§5①'）。読んでから始めること。**
投信 `0331418A` の `tech-adx` は True Range が |ΔC| に潰れたまま
**ADX 13.64 / +DI 51.54 / −DI 48.46 と、それらしい数値を出して「レンジ相場」と断定する。**
残る3件も同じ理屈（いずれも高安を使う）なので、**4件とも UNAVAILABLE 方向で始めてよい。**
それでも**画面は一度見ること**（§0.5③ と同じ失敗の形を避けるため）。

**S15 のバッジと関係がある（§0.6③）。**
`tech-adx` / `tech-breakout` / `tech-stoch` の3件には S15 がバッジを付けた。
現状の main では投信でもこの3つが SAFE なので、
**偽の ADX から作られたバッジが閉じた見出しにまで出ている。**
UNAVAILABLE に倒すと `AccordionSection` が `summary` を渡さなくなり**自動的に消える**ので
追加のコードは要らない。**消えることを画面で確認すること。**

### FU27: discretionary 節がパネルIDを持たない
DiscretionaryLab を直接描くため AccordionSection の外にある。
**FU17 の台帳にも、S15 のバッジにも、共有URLにも乗らない。**
他節と同じ土台に載せるか、載せない理由を記録するかを決めること。
**S19 の M3（レジストリ化）で必ず問題になるので、S19 への申し送りを書くこと。**

## 制約
- パネルID は不変（共有URL・localStorage 互換）
- AnalysisGuide 必須規約は維持
- 台帳を変えたら page-wiring.test.ts が通ること（全251IDがちょうど1分類）

## 受け入れ条件
1. 5件それぞれについて「直した / 直さないと決めた」と理由が記録されている
2. FU26 は**投信の画面を見た結果**として判定されている（スクリーンショット付き）
3. **FU26 で UNAVAILABLE に倒したパネルは、S15 のバッジも消えていること**（投信の画面で確認）
4. FU20 は sitemap の外部取得を増やしていない。その設計判断が報告されている
5. FU23 の catch が握りつぶす範囲が絞られ、絞った根拠が書かれている
6. **U2 を決めるとき、S15 が入れた `summaryScope`（銘柄＋期間＋最終足）の破棄規則と
   矛盾しないこと。** 開閉状態は銘柄をまたいで残り、バッジは銘柄が変わると消える。
   **この非対称を「そのままでよい」とするなら、理由をコードに残すこと**
7. npm test / npm run lint / npm run build が通る
```

---

### ~~S18~~ — Canvas 代替テキスト（A3）＋ A4 の残り※**完了 2026-08-28**（`c3c568f`）。**A3 は 150件中10件で打ち止め。残りは S20** → §0.7①

```
作業ディレクトリ: C:/Users/hikar/next/sa-s18
ブランチ: feat/canvas-alt-text

［共通ひな形をここに貼る］

## 担当
docs/site-improvement-execution-plan.md の A3（§3 グループA）と、
**A4 の残件**（本書 §0.5⑤）。S16 が土台を作った直後なので、その上に積むこと。

## 専有ファイル
- Canvas 描画を持つ分析コンポーネント群
- app/lib/chart-colors.ts

**触らないこと:**
- app/sitemap.ts / app/lib/panel-*.ts（S17 の専有）
- app/page.tsx の構造（S19 の専有）
- **`CollapsibleAnalysis.tsx` / `AccordionSection.tsx`**（S15 がバッジ用に触った直後。
  永続層は S17、構造は S19 の担当。**A3 のためにここを開く必要は無いはず**）

## S15 が入れたバッジと衝突しないこと（§0.6③）
見出しのバッジは既に **記号＋色＋`aria-label`** を持っている。
A3 の代替テキストを足すとき、**同じパネルで同じ内容を二重に読ませないこと。**
色は `DIRECTION_TEXT_CLASS` に統一済みなので、**新しい方向色を作らないこと。**

## S16 が用意した土台（**新しい仕組みを作る前に読むこと**）
- `app/lib/chart-colors.ts` — CHART_COLORS / DIRECTION_COLORS / DIRECTION_TEXT_COLORS /
  DIRECTION_GLYPH / DIRECTION_LABEL / directionOf / withSign / CANDLESTICK_OPTIONS
- `app/components/analysis/DirectionValue.tsx` — 記号＋符号＋aria-label の共通表示
- **配色方針は決定済み（米国式維持）。§0.5④。再検討しないこと**

## A4 の残り（先にこちら。範囲が確定している）
S16 が入れたのは**基本・テクニカル・OHLC・リスクの4節、表37箇所**だけである。残りは:
1. 残り14節の表・凡例
2. Canvas 内で系列を色だけで区別している箇所（凡例に形・線種が無い）

**37箇所と同じやり方（DirectionValue / DIRECTION_GLYPH）で機械的に広げること。**
ただし S16 が見つけた罠を引き継ぐこと:
- **中立点が 0 でない指標は、中立点を引いた値を渡す**（勝率→x−0.5、
  オメガレシオ・MFE/MAE比→x−1）。そうしないと記号が常に片側に倒れる
- **方向を持たない指標（σ・尖度など）には記号も色も付けない**
- **すでに語で区別している箇所は変更しない**（「上昇トレンド」/「下降トレンド」等）

## A3: Canvas の代替テキスト（本題）
Canvas は中身がアクセシビリティツリーに出ない。**読み上げ利用者には図が存在しない。**

**進め方の指定:**
1. **まず対象を数えて分類して報告すること。** Canvas を持つコンポーネントが何件あり、
   そのうち「図が唯一の情報源」なのは何件か（表や数値が併記済みなら優先度は低い）
2. **全件に長文の説明を書かせないこと。** 各分析は既に AnalysisGuide を持っており、
   「手法の説明」はそこにある。A3 が足すべきは**この図が今この銘柄で何を示しているか**
   （例: 「ラグ1で0.08、以降は95%帯の内側。自己相関は検出されていない」）
3. 実装方式（`role="img"` + `aria-label` / 併設の視覚的に隠したテキスト /
   `<figcaption>`）を**先に決めて確認を取ること**

**罠: aria-label を素の span/canvas に付けるだけでは不十分である。**
S16 の DirectionGlyph は `<span aria-label="上昇">▲</span>` の形で、
ARIA 的には role=generic に aria-label を付けている。Chrome/NVDA では読まれるが
仕様上は保証されない。**S18 で `role="img"` か sr-only テキストへ寄せ直すこと**
（これは S16 の実装を直す作業でもある。1ファイルで済む）。

## 制約
- **CLAUDE.md のチャート規約を守ること。** この作業で描画方式を変えないこと
- 代替テキストは**計算結果から生成すること**。固定文言を貼らない
  （銘柄が変われば内容が変わらなければ意味がない）
- AnalysisGuide 必須規約は維持
- パネルID は不変

## 受け入れ条件
1. A4 の残件が「対応した / 対応不要と判断した」で全件分類されている
2. Canvas を持つコンポーネントの棚卸し表がある（件数・図が唯一の情報源か）
3. 代表的な分析で、**読み上げに実測値が乗る**（スクリーンリーダーまたは
   アクセシビリティツリーの表示で確認したスクリーンショット）
4. DirectionGlyph の aria-label が role="img" か sr-only に寄せ直されている
5. npm test / npm run lint / npm run build が通る
```

---

### S19 — パネルのレジストリ化（M3）※**必ず単独**

```
作業ディレクトリ: C:/Users/hikar/next/sa-s19
ブランチ: feat/panel-registry

［共通ひな形をここに貼る］

## 担当
docs/site-improvement-execution-plan.md の M3（§3 グループM）。
**app/lib/panel-sections.ts と app/lib/panel-data-requirements.ts を吸収する。**

## なぜ単独なのか
app/page.tsx の構造を組み替える。**他のどのセッションとも並列にできない。**
S15（バッジ）・S17（台帳の増減と一括開放）・S18（代替テキスト）が入った後の
「最終形」を1つのレジストリにまとめるのが目的である。
（S20 = A3 の残りはこの後に単独で投げる。並列にしない）

## ★ 第3波（S17 / S18）で前提が3つ変わった（§0.7）

**① `AccordionSection.tsx` はもう「触っていないファイル」ではない。**
S17 が U2（一括開放を銀柄をまたがせない）のために、
`CollapsibleAnalysis` の React key へ S15 の `summaryScope` を混ぜた
（`key={パネルID}:{summaryScope}` の形。`AccordionSection.tsx` の2箇所）。
これは**銀柄・期間・最終足のどれかが変わるたびに全パネルを再マウントする**という意味である。
実際、初回ロード直後に「すべて開く」を押すと直後に全部閉じる現象が1回再現している。
**FU35（要約の per-panel 購読）と同時に見直すこと** → **FU41**。
一括開放の寿命を別の手段で保つなら、**U2 の仕様（一括開放は永続しない・個別開閉は永続する）を変えないこと**。

**② 台帳の分類が4件動いた。**
`tech-adx` / `tech-breakout` / `tech-stoch` / `vol-atr` が SAFE → UNAVAILABLE。
現在は **UNAVAILABLE 78 / CAUTION 24 / SAFE 149**である（合計 251 は不変）。

**③ `discretionary` 節の扱いが決まった（FU27）。**
擬似パネルID を作らず、**「AccordionSection 外の節エントリ」を表せる種別**を
レジストリに用意すること。共有URLと結果バッジの対象外である点も明示的に保つ。
理由は `app/lib/panel-data-requirements.ts` の冒頭にある。

## 現状ばらばらに置かれているもの（**着手前に全部読むこと**）
| 場所 | 内容 |
|---|---|
| `app/page.tsx` | 251件の `id:` とセクション構成・コンポーネント配線 |
| `app/lib/panel-sections.ts` | セクションの定義 |
| `app/lib/panel-data-requirements.ts` | UNAVAILABLE **78** / CAUTION 24 / SAFE **149** の3分類（S17 が4件動かした） |
| `page-wiring.test.ts` | 全251IDがちょうど1分類に属することの検査 |
| **`useAnalysisResultSummary`**（S15・実装済） | 各パネルの要約・バッジ供給。**IDを文字列で複製している**（FU33） |
| （S18 が足す）| 各パネルの代替テキスト供給 |

## 着手前にやること（S15 の結果を受けて追加。FU33）
**`useAnalysisResultSummary("tech-adx", …)` のリテラルIDが `app/page.tsx` の台帳と
一致することを検査するテストを、先に足すこと。** 今は7箇所だが**テストが無いので、
IDを動かすとバッジが無言で出なくなる。** M3 はまさにIDの所在を動かす作業である。
`app/lib/__tests__/page-wiring.test.ts` が既に `page.tsx` をソースとして読んでいるので、
そこに `useAnalysisResultSummary(` の第1引数を集める検査を追加するのが最短。
**この安全網を張ってから移設を始めること。**

## 先に決めて確認を取ること
**① バッジは「判断」か「量」か**（§0.6④）。現状は `売られすぎ` と `標本 231件` が
同じ見た目で混在している。**レジストリに要約の型を持たせるなら、ここで揃うべきである。**
揃えるか、揃えないと決めるかを報告すること。

**② 1パネル1レコードにできるか。** 現状 page.tsx はコンポーネントを JSX で直に配置しており、
**動的 import（next/dynamic）と型が絡む。** レジストリを「データ」にすると
`next/dynamic` の静的解析が効かなくなる可能性がある。
**この点を先に実測して報告すること**（ビルド出力のチャンク分割が変わらないこと）。

## 罠
- **discretionary 節はパネルIDを持たない**（FU27。S17 が判断を残しているはず）。
  レジストリの型を決める前にその判断を読むこと
- **SAFE は「検証済み」ではない**（§0③）。移設時点の挙動を写しただけである。
  レジストリ化で「検証済み」に見える名前を付けないこと
- **`chart-colors.ts` は `globals.css` と値を二重に持っている**（§0.5⑥）。
  レジストリ化のついでにテストで縛れないか検討すること
- **パネルID は不変**（共有URL・localStorage 互換）。移設で1つも変えないこと。
  **既存の page-wiring.test.ts を消さずに通すこと**が最大の安全網である

## 受け入れ条件
1. パネルの定義が1箇所に集約され、page.tsx から重複が消えている
2. **パネルIDが1つも変わっていない**（テストで検査されている）
2.5 **バッジのIDもテストで縛られている**（FU33。移設の前に張ること）
3. ビルド出力のチャンク分割が悪化していない（**前後の実測値を報告**）
4. 新しいパネルを足す手順が `.claude/skills/add-analysis/SKILL.md` と整合している
   （現在「配線は5か所」。レジストリ化で減るはず。**手順書も更新すること**）
5. npm test / npm run lint / npm run build が通る
```

---

### ~~S19 への申し送り~~（第3波の結果を受けて追記。**S19 のプロンプト本体に反映済み**）

第3波（S17 / S18）が main に入った。**S19 の前提が3つ変わった。**

1. **`AccordionSection.tsx` はもう「触っていないファイル」ではない。** S17 が
   `CollapsibleAnalysis` の React key に `summaryScope` を混ぜた（§0.7③④）。
   **S19 はこの key を FU35（要約の per-panel 購読）と同時に見直すこと。**
2. **台帳の分類が4件動いた。** `tech-adx` / `tech-breakout` / `tech-stoch` / `vol-atr` が
   SAFE → UNAVAILABLE。**UNAVAILABLE 78 / CAUTION 24 / SAFE 149 になっている。**
3. **`discretionary` 節の扱いが決まった**（FU27）。擬似パネルIDを作らず、
   **「AccordionSection 外の節エントリ」を表せる種別**をレジストリに用意すること。
   理由は `panel-data-requirements.ts` の冒頭にある。

---

### S20 — Canvas 代替テキストの残り（A3）＋ 記号と色のずれ（FU36 / FU37）※**S19 の後・単独**

```
作業ディレクトリ: C:/Users/hikar/next/sa-s20
ブランチ: feat/canvas-alt-text-rest

［共通ひな形をここに貼る］

## 担当
docs/site-improvement-execution-plan.md の A3 の残り（本書 §0.7①）と、
S18 が作り込んだ記号の不整合 **FU36 / FU37**（§0.7②）。

## 専有ファイル
- Canvas 描画を持つ分析コンポーネント群
- app/lib/chart-colors.ts

**触らないこと:**
- app/page.tsx / app/lib/panel-*.ts（S19 が組み替えたばかり）
- CollapsibleAnalysis.tsx / AccordionSection.tsx

## ★ 最初のコミットで棚卸し表を出すこと（ここが受け入れ条件の要）
S18 は棚卸しを作らずに10件だけ実装したため、**「A4 の残件を全件分類した」という
条件そのものが検算不能になった**（§0.7①）。同じことを繰り返さないために:

**1コミット目は「棚卸し表だけ」にすること。** 実装を1行も含めないこと。
`docs/canvas-alt-text-inventory.md` に次の列で全件を並べる:

| コンポーネント | パネルID | 節 | 図が唯一の情報源か | S18 で対応済みか | 今回の扱い |

- 母数は `getContext("2d")` を持つコンポーネント **150件**（2026-08-28 時点）。
  **この数え方自体を疑うこと。** `<canvas` を持つファイルは 173件ある。
  差分が何なのかを説明してから始めること（§0.5③ の形）
- **「図が唯一の情報源か」の判定基準を先に書くこと。** 表や数値が併記済みなら優先度は低い
- **全件に長文を書かせない。** A3 が足すのは「この図が今この銘柄で何を示しているか」であって
  手法の説明ではない（それは AnalysisGuide にある）

## S18 が作った土台（**新しい仕組みを作らないこと**）
- `app/components/analysis/AccessibleCanvas.tsx` — `description` 必須の canvas ラッパ。
  `role="img"` + `aria-label` を付ける。**これをそのまま使う**
- 実装済みの10件を手本にする（`WaveletChart` / `EntropyHeatmapChart` /
  `WaveletCoherenceChart` ほか）。説明文は `useMemo` で**計算結果から生成**している

## FU36: 記号と色が同じ判定から出ていない（先にこちらを直す。範囲が確定している）
S18 は `DirectionGlyph` を184箇所に足したが、**`eps` を渡した箇所が0件**である。
一方、既存の色付けには不感帯がある:

| 箇所 | 色の閾値 | 記号の閾値 |
|---|---|---|
| `PersistenceChart` の相関 | `>0.5` 緑 / `<0` 赤 | `0` |
| 勝率（複数箇所） | `>0.52` / `<0.48` | `0.5` |
| IC | `\|ic\|>=0.1` | `0` |
| `signShare` / `posShare` | `>0.6` / `<0.4` | `0.5` |

**色が灰色（中立）の範囲で記号だけが方向を主張している。**
A4 の趣旨は「色が見えなくても同じ結論に達する」ことなので、これは趣旨を壊している。
**`eps` を渡して色と記号を同じ判定から出すこと。**

## FU37: 方向を持たない量に記号が付いた
最大ドローダウンは定義上つねに ≤0 なので **▼が常時点灯する**（`VolTargetingChart` ほか3箇所）。
**記号を外すこと。** 差（`strat.maxDD - bh.maxDD`）は符号を持つので残してよい。

**ついでに全184箇所を見直すこと。** 「σ・尖度など方向を持たない量に付いていないか」
「すでに語で区別している箇所に重ねて付いていないか」を1件ずつ判断する。
**機械的に広げられない作業である**（§0.7② の教訓）。

## FU39: chart-colors.ts を使わずハードコードした色を戻す
S18 が凡例に線種を足した3ファイル（`RegimeTransitionChart` / `TDAChart` /
`TpSlOptimizerChart`）で `#16a34a` / `#dc2626` を**リテラルで複製**している。
`DIRECTION_COLORS` と同値なので import に置き換えること。

## 制約
- **CLAUDE.md のチャート規約を守ること。** 描画方式を変えないこと
  （`createChart` 142件・`getContext("2d")` 173件が変わらないことを報告に含める）
- 代替テキストは**計算結果から生成すること**。固定文言を貼らない
- AnalysisGuide 必須規約は維持
- パネルID は不変
- **S15 のバッジと同じ内容を二重に読ませないこと**（§0.6③）

## 受け入れ条件
1. **1コミット目が棚卸し表だけである**（実装を含まない）。母数の数え方の説明がある
2. 棚卸し表の全件が「対応した / 対応不要と判断した」に分類され、**残件が0か、
   残件の件数と理由が表に書かれている**
3. FU36 が全184箇所で解消され、**色と記号が同じ判定から出ている**
4. FU37 の常時点灯が解消されている
5. 代表的な分析3件で、アクセシビリティツリーに実測値が乗る（スクリーンショット）
6. 描画方式の件数が変わっていない
7. npm test / npm run lint / npm run build が通る
```

## 4. マージと片付けの手順

> **第1波（S14 / S16）は 2026-08-21 に、第2波（S15）は 2026-08-23 に実施済み。**
> 以下は第3波以降の手順として読むこと。

### 別セッションにやらせる場合（推奨）

**`docs/review-merge-prompt.md` にそのまま貼れるプロンプトがある。**
実装セッションとは別の、新しいセッションに投げること（**実装した本人に検算させない**）。
推奨推論レベルは **high**。

```powershell
Get-Content docs/review-merge-prompt.md -Raw | Set-Clipboard
```

**作業ディレクトリは親リポジトリ**（`C:\Users\hikar\next\stock-analysis`）であって
worktree ではない。マージも片付けも文書更新も親側の作業である。

このプロンプトは「マージして終わり」ではなく、**§0.7 の新設・§3 の見出しの取り消し線・
§6 のフォローアップ追加・プロンプトの再生成・push まで**を担当範囲に含めてある。
`docs/prompts/` は生成物なので、**§3 の見出しを `~~S17~~` の形にしないと
次の生成でプロンプトが出続ける。**

### 手つきでやる場合

```powershell
# 1. Codex セッションを閉じる（開いたままだと worktree 削除が失敗する）

Set-Location C:\Users\hikar\next\stock-analysis

# 2. ★ マージ前に「意味の整合」を確かめる（§0.5① の教訓）
#    無衝突マージは整合を保証しない。分岐後に main が触った概念を作業側でも grep する
git log --oneline "$(git merge-base main <BRANCH>)..main"   # 分岐後に main へ入ったもの
git merge-tree --write-tree main <BRANCH>                   # 衝突の有無だけ先に見る

git merge --no-ff <BRANCH>

npm test; npm run lint; npm run build
npm start   # ★ dev ではなく本番ビルドで、変更した画面を実際に開くこと

git worktree remove ..\<DIR>
git worktree prune
git branch -d <BRANCH>

# 進捗表（§11）を人間側でまとめて更新
# ★ worktree remove の後、..\<DIR> のフォルダが残っていないか目視すること
#   （sa-s10 が空き殻として残った前例がある）
```

### 第1波でこの手順に足りなかったこと

`git merge` が無衝突で通ったので**そのまま受け入れそうになった。**
実際には `opengraph-image.tsx` が `range=1y` に取り残されていた（§0.5①）。
**上の手順2（`merge-base..main` を読んでから作業側を grep）を必ず挟むこと。**

---

## 5. 実装以外にした方がよいこと

優先順に並べた。**①と②は S15 を投げる前に済ませること。**

> **2026-08-21 追記:** 第1波のマージで新しく増えた項目は ⑧〜⑪。
> ①〜③は**第1波のぶんも含めて未実施のまま**なので、内容を読み替えること
> （①の確認リストに OG画像の行を追加した）。

### ① main を push して本番で確認する 〜**2026-08-23 実施済み。結果を下に記録**〜

> **push 済み**（`90cdd7a..82a1304`）。本番 `https://kabugenron.com` で下表を実測した。
> **不具合なし。** ただし新しい所見が2件出た（→ FU26 の答え・FU32）。

| 確認 | 実測結果 |
|---|---|
| OG画像が本番で描けるか | **200 / image/png / 94,856 B。** 最大の懸念だった**フォントの base64 が Vercel のビルドを通った** |
| OG画像の日本語 | **豆腐なし。** 「トヨタ自動車」「年率ボラティリティ」「2026年8月21日時点」すべて正常 |
| OG画像の応答 | **初回 4.07秒（MISS）→ 0.20秒。** 3回目以降 `x-vercel-cache: HIT`、`Age` が伸びる |
| OG画像の CDN | **載っている。** S14 の `s-maxage=28800` は有効（HEAD/GET が別エントリなので初回2回は MISS に見える） |
| `/t/7203` の CDN | **MISS → HIT**（`Age: 13` → `39`）。`7126c26` の `Vercel-CDN-Cache-Control` が効いている |
| `/t/7203.T` | **308 → `/t/7203`**（`cf09c48` の FU19 修正が本番で効いている） |
| `/sitemap.xml` | `<loc>` **129件**（109から増。`/guide/[slug]` の増加ぶん） |
| **投信の FU17** | **効いている。** `0331418A` の OHLC節「ローソク足構造分析」で「始値・高値・安値が基準価額と同一で、出来高も0です…結果を表示しません」を確認 |
| チャートの見た目の退行 | ADX・ローソク足・出来高を本番で確認。**軸ラベルが濃くなった以外の変化なし** |

**まだ残っているのは、私が代行できない2件だけ:**
- **SNS のプレビュー**（X / Slack に貼る）— アカウントでの投稿になるのでオーナーが行うこと。
  `og:image` / `twitter:image` / `summary_large_image` が正しく出ていることは HTML で確認済みなので、
  **残るリスクは各SNSのクローラが CDN を BYPASS したときの初回タイムアウトだけ**である
- **Search Console の sitemap 再送信**（→ ②）

---

### ①' 本番で新しく分かったこと

**FU26 の答えが出た（S17 は調査から始めなくてよい）**

投信（`0331418A`）で `tech-adx` を開いたところ:
- ローソク足が**高さゼロの点列**になる（H=L=O=C なので実体もヒゲも無い）
- S16 が入れた凡例「中空（白抜き）＝陽線・上昇／塗りつぶし＝陰線・下落」が
  **意味を持たないまま表示される** → 新規 **FU32**
- **ADX 13.64 / +DI 51.54 / −DI 48.46 と、それらしい数値が出てしまう。**
  True Range が |ΔC| に潰れるので、これは ADX ではない別物である
- そのうえで「レンジ相場 / 上昇トレンド」と**断定を表示している**

→ **`tech-adx` は UNAVAILABLE が妥当。** `tech-breakout` / `vol-atr` / `tech-stoch` も
同じ理屈（いずれも高安を使う）なので、S17 は**4件まとめて UNAVAILABLE に倒す**方向で
始めてよい。§0③ の「SAFE は検証済みという意味ではない」がそのまま実証された。

**FU32: ローソク足の凡例が、ローソク足として成立しない銘柄にも出る**

S16 は「凡例を出さないと第2の手がかりが伝わらない」として各チャート直下に
固定の凡例文を置いた。**投信では中空も塗りつぶしも存在しない。**
FU26 で該当パネルを UNAVAILABLE にすれば大半は消えるが、
**基本節のローソク足は投信でも表示される**ので別途の手当てが要る。



`/t/[ticker]` は **98の新規URLを公開する変更**である。ローカルの dev で確認したが、
本番の挙動は違いうる（Runtime Cache・brotli・middleware）。
`docs/site-improvement-round3.md` 以降の通り、**確認は Vercel デプロイ版で行う**。

デプロイ後に確認すること:

| 確認 | 見るもの |
|---|---|
| `/t/7203` が 200 で数値入りHTMLを返すか | ブラウザのソース表示（JS無効でも数値が見えるか） |
| 初回と2回目の応答時間 | Runtime Cache の HIT/MISS で差が出るはず |
| **CDN に載っているか** | `curl -sSI https://kabugenron.com/t/7203` の `x-vercel-cache` が MISS → HIT に変わること |
| **キャッシュ共有が効いているか** | `/` でトヨタを表示した**あと**に `/t/7203` を開き、初回から速いこと（§0① の本題） |
| `/t/7203.T` | **308 → `/t/7203`**（ローカルでは確認済み。本番の proxy.ts と併存するか） |
| `/sitemap.xml` に109件あるか | `/t/` の行数 |
| 存在しない銘柄・上場廃止銘柄 | `noindex, follow` の代替表示が出るか（500 にならないこと） |
| **投信（`0331418A`）での FU17** | 出来高パネル・OHLCパネルに「表示しません」の注意書きが出るか。**ここはローカルで目視していない** |
| 株式（`7203.T`）での FU17 | 従来どおり全パネルが出ること（退行していないこと） |
| 投信の 500 再発 | FU14 の退避が効くか。`03311187`（`&` を含む名前）で複数期間を叩く |
| **OG画像が本番で描けるか** | `curl -sSI https://kabugenron.com/t/7203/opengraph-image` が 200/PNG。**ローカルでは描けたが、フォントの base64 が Vercel のビルドを通るかは未確認** |
| **OG画像の初回応答** | ローカルで 1.99秒（MISS）→ 0.10秒（HIT）。本番の実測値を取ること |
| **SNS のプレビュー** | X / Slack / Discord に `https://kabugenron.com/t/7203` を貼って**大カードで画像が出るか**。ここが R3b の本当の受け入れ条件である |
| **クローラに CDN が効かないことの再確認** | §0① のとおり検証済みクローラは BYPASS する。OG画像の `s-maxage=28800` も**SNS のクローラには効かない可能性がある**。実測して §0.5 に追記すること |
| **チャートの見た目の退行** | S16 が Canvas の色を全面的に変えた。**代表的な図を本番で数枚見ること**（軸ラベルが濃くなった以外の変化が無いか） |

### ①'' S15 のマージぶんがまだ本番に無い（**次の push で確認すること**）

`fe392ab`（S15）を含む **3コミットが `origin/main` より先行している。**
push したら本番で下の3点だけ見ること。ローカルでは見えない類のものではないが、
**バッジは全節の共通土台に載っているので、退行すると全画面に出る。**

| 確認 | 見るもの |
|---|---|
| バッジが出る | `7203` テクニカル節。パネルを開いて閉じ、**見出しに `▲買い所見` 等が残る** |
| バッジが消える | 銘柄を変える／期間を変える → **前の銘柄のバッジが残っていない** |
| 非対応と競合しない | 投信 `0331418A` のテクニカル節で、非対応パネルにバッジが出ていない |
| サマリーカード | 冒頭「期間変動」が **`▲+12.34%`** の形（記号＋符号）になっている |

**バッジはリロードで消える**（localStorage に保存していない）。これは仕様である → §0.6⑤。

---

### ①''' 第3波（S17 / S18）を push した後に本番で見ること

**S15 のぶん（①''）もまだ本番に無い。** `fe392ab` 以降がまとめて本番に出るので、
①'' の4点と合わせて下表を見ること。**優先度の高い順に並べた。**

| 確認 | 見るもの | なぜ |
|---|---|---|
| **投信の FU26** | `0331418A` のテクニカル節で `tech-adx` / `tech-stoch` / `tech-breakout`、ボラ節で `vol-atr` が「結果を表示しません」になり、**バッジも出ていない** | 本番ビルドではローカルで確認済み。**本番の投信取得は経路が違いうる** |
| **株式の非退行** | `7203` で同じ4パネルが従来どおり数値を出し、`tech-adx` にバッジが出る | UNAVAILABLE 判定が株式に漏れていないこと |
| **FU23 の narrowing**（★新しい失敗の入口） | **古い投信・償還間近の投信を1本**開く。500 や「データがありません」にならないか | BFF が空配列を返したときの 404 が **RSC へ退避しなくなった** → §0.7⑥ |
| **U2 の永続範囲** | `すべて開く` → 別銘柄を検索 → **一括開放が引き継がれていない**。個別に開いたパネルは開いたまま | 仕様どおりか、本番の再取得タイミングで壊れないか |
| **一括開放の取りこぼし** | 検索直後（データ確定前）に `すべて開く` を押して、**直後に閉じてしまわないか** | ローカルで1回再現した現象。本番のほうが取得が遅いので出やすい → §0.7④ |
| **Canvas の代替テキスト** | `freq-wavelet` を開き、DevTools の Accessibility パネルで canvas が `img` ロール＋実測値入りの名前を持つか | 10件だけなので、他の Canvas に読み上げが無いのは**現時点では仕様** |
| **記号と色のずれ** | 勝率が 0.50〜0.52 のセルで、**色は灰色なのに ▲ が出ていないか**（FU36 の実害の確認） | S20 の優先度を決める材料になる |
| **チャートの見た目** | 凡例に線種を足した3図（レジーム遷移・TDA・TP/SL 最適化）を見る | S18 が Canvas 描画に手を入れた3ファイル |

**S17 / S18 とも、ローカルの本番ビルド（`npm start`）では上の1〜2行目と6行目を実測済み。**
残りは本番でしか分からない。

---

### ② Search Console に sitemap を再送信する

URL が 11 → 109 に増えた。**再送信しないと反映が遅れる。**
R1 の目的（クロール可能面を増やす）が達成されたかは、
**2週間後にインデックス数を見て初めて分かる。** 日付をカレンダーに入れておくこと。

### ③ 銘柄ページの掲載値が変わったことを承知しておく

取得を `10y` に寄せた際、窓の定義が「Yahoo の `range=1y`」から
「最終観測日から1年」に変わった。`7203` の実測:

| | 変更前 | 変更後 |
|---|---|---|
| 期間リターン | +10.95% | **+6.96%** |
| 年率ボラティリティ | 30.65% | 30.62% |
| 最大ドローダウン | −30.88% | −30.88% |

**ボラと最大DDがほぼ不変で、リターンだけ動いた**のが期待どおりの符号
（窓の始点だけがずれ、中身は同じ）。値が変わること自体は正しい。

### ④ FU7 を目視で片付ける（5分で終わる）

サマリーカードの桁が「2,914.5」と「3,452.59」のように不揃いかどうか。
**実機を見るだけで決まる項目**が、実機を見ていないという理由だけで残り続けている。
`/t/[ticker]` でも `formatSummaryPrice` を使うようになったので、対象が増えた。

### ⑤ FU3（lint 272警告）を落とす作業の枠を決める

CI は警告で落ちない設定なので実害はないが、272件あると**新しい警告が埋もれる**。
機械的な作業なので、S16 のような大きなセッションの「ついで」ではなく、
**単独の小さいセッションで一気に片付けるほうが安全**（差分が巨大になるため）。

### ⑥ FU1 の後始末（オーナートークンが要る）

`GET https://kabugenron.com/api/waitlist?token=<FEEDBACK_ADMIN_TOKEN>` で
`LOWER(email)` の一意制約が効いて1行だけになっているか確認し、
**投入したテスト行 `fu1-verify-20260812@example.com` を削除する。**
オーナーにしかできない作業として2026-08-11から残っている。

### ⑦ 〜完了（2026-08-17）〜 `add-analysis` の手順に台帳を追記する

`.claude/skills/add-analysis/SKILL.md` §4 を「配線は4か所」→「5か所」にし、
`(e) panel-data-requirements.ts に分類を足す` を追加した。
分類しなければ `npm test` が落ちるので、手順書を読み飛ばしても止まる。

---

### ⑧ 銘柄を入れ替えたら OG画像のフォントを焼き直す（**忘れると静かに退化する**）

`app/t/[ticker]/_fonts/` のサブセットは **`TICKER_PAGE_INSTRUMENTS` 98銘柄の名前に
厳密に一致**させてある（284字）。銘柄を1つ足すと、その銘柄だけ
**数値の入ったカードが出ず汎用画像に落ちる**（豆腐は出ない。壊れないので気づかない）。

焼き直し手順は `_fonts/README.md` にある（Python + fonttools）。
**これは FU20 と同じ「手動台帳が腐る」形である。**
`OG_FONT_CHARSET` が全銘柄名を被覆していることを検査するテストを足すのが本筋
（**S17 か S19 に投げる候補**）。実際、今回のレビューではその照合を手で走らせた
（98銘柄・未収録0字）。

### ⑨ A4 の配色決定を親文書 §8 に反映する

§8 の V7・V8 が「未決定」のまま残っている。**決定済み（米国式維持）**へ更新し、
理由は `app/lib/chart-colors.ts` を参照させること（§0.5④）。
放置すると次のセッションが同じ論点を蒸し返す。

### ⑩ FU3（lint 272警告）の枠は「まだ」でよい

第1波で警告は**1件も増えていない**（272 → 272）。S16 が254ファイルを触っても
増えなかったので、この数字は安定している。**⑤の判断は据え置きでよい。**
ただし S19（M3）は page.tsx を組み替えるので、**S19 の前に片付けるほうが差分が読みやすい。**

### ⑪ `chart-colors.ts` と `globals.css` の二重管理をテストで縛る

§0.5⑥。Canvas は CSS 変数を読めないため同じ値を2箇所に持っている。
ファイル冒頭に対応表があるだけで、**片方を変えても何も落ちない。**
`globals.css` の `@theme inline` を読んで突き合わせるテストを1本足せば済む。
**S18 か S19 のついでで構わないが、誰の担当か決めること。**

---

## 6. 未処理のフォローアップ（親文書 §11 が正）

| # | 内容 | 状態 |
|---|---|---|
| FU1 | 待機リストの行数確認・テスト行削除（要オーナートークン） | 一部完了 → **§5⑥** |
| FU2 | 待機リスト API のレート制限 | 未 |
| FU3 | lint の既存272警告 | 未 → **§5⑤** |
| FU4 | J8 を活かす新機能の設計 | 未設計 |
| FU6 | `format.ts` の未使用関数・非JPYの `$` 決め打ち | 未 |
| FU7 | サマリーカードの桁の見た目 | 未（要目視）→ **§5④** |
| FU11 | A1 の残46箇所＋`app/axioms/page.tsx` | **完了 2026-08-21**（S16）。46件を本文20/装飾26に振り分け全件是正。`text-gray-400` 残0 |
| FU12 | Canvas 内ハードコード灰色 386箇所 | **完了 2026-08-21**（S16）。`app/lib/chart-colors.ts` へ集約。**短縮表記65箇所の取りこぼしも塞いだ**（§0.5③）。低コントラスト灰色 残0 |
| FU15 | 投信10年が初回8.0秒 | 未（`/t/[ticker]` は**株式のみ**なので当面影響なし。ただし取得は 10y に変わった） |
| FU18 | 破損点検パネルの全節対応 | 完了（**画面での目視は未実施** → §5①） |
| FU19 | `/t/7203.T` の正規化リダイレクトが死んでいる | **完了 2026-08-17**（`cf09c48`。`dynamicParams = true`） |
| FU20 | sitemap が価格取得可否を検証しない | **判断済 2026-08-28**（S17）。**外部取得を増やさない**と決め、理由を `sitemap.ts` に記録（コード変更は無し）。**台帳が腐る問題は未解決** → **FU40** |
| FU21 | `/t/[ticker]` が SSG されない・初回3.35秒 | **完了 2026-08-17**（`cf09c48`/`7126c26`。ISR化せず、キャッシュ共有＋CDNヘッダで対処） |
| FU22 | FU17 の台帳が `page.tsx` にハードコード | **完了 2026-08-17**（`6378813`。3分類＋全数被覆テスト。M3=S19 で本物のレジストリに吸収すること） |
| FU23 | FU14 の退避 catch が広すぎる | **完了 2026-08-28**（S17）。`shouldFallbackFromFundBff()` で 401/403/408/425/429/5xx のみ退避。404 と実装例外は再送出 → §0.7③ |
| FU26 | `SAFE` に高安を要するパネルが4件混ざっている | **完了 2026-08-28**（S17）。4件とも **UNAVAILABLE** へ。投信 `0331418A` の本番ビルドで「結果を表示しません」に変わり、**S15 のバッジも自動的に消えた**ことを確認 → §0.7① |
| FU27 | `discretionary` 節はパネルID を持たず、終値だけの系列の分類の外にある | **判断済 2026-08-28**（S17）。**擬似パネルID は作らない**。S19 は「AccordionSection 外の節エントリ」を表せる種別を用意すること |
| FU24 | 系列セレクタが節単位・実態はパネル単位（simulation 2/17・quantum 1/6 等） | **未着手。S15 は触らなかった** → **S19 へ移す**（パネル単位の属性なのでレジストリの1フィールドで済む） |
| FU25 | パネルID→節の解決が無く共有URL `?panel=` が無言で失敗 | **完了 2026-08-15**（`panel-sections.ts`。M3=S19 で本物のレジストリに吸収すること） |
| **FU28** | OG画像のフォントサブセットが `TICKER_PAGE_INSTRUMENTS` と手動同期（銘柄追加で静かに汎用画像へ退化） | 新規 → **§5⑧**。テストで縛るのが本筋 |
| **FU29** | `app/lib/chart-colors.ts` と `globals.css` の `@theme inline` が値を二重に持つ | 新規 → **§5⑪** |
| **FU30** | A4 の第2の手がかりは4節37箇所どまり | **一部完了 2026-08-28**（S15・S18）。S18 が表に184箇所追加。ただし**分類表が無く「全件分類」は未達**で、**Canvas 内の色のみの区別は3ファイルのみ** → **S20** |
| **FU31** | `DirectionGlyph` の `aria-label` が role=generic な span に付いている | **完了 2026-08-28**（S18。`role="img"` を付与） |
| **FU32** | ローソク足の凡例（中空/塗りつぶし）が、H=L=O=C で実体を持たない投信にも表示される | 新規 → **§5①'**。FU26 で大半は消えるが**基本節のローソク足は投信でも出る** |
| **FU33** | `useAnalysisResultSummary("tech-adx", …)` がパネルIDを文字列で複製している（7箇所）。`page.tsx` でIDを変えるとバッジが**無言で消える** | 新規 → **S19 の着手前にテストで縛る**（§0.6②） |
| **FU34** | バッジの粒度が「判断（売られすぎ）」と「量（標本231件）」で混在している | 新規 → **S19 でレジストリの型を決めるときに揃える**（§0.6④） |
| **FU35** | 要約が1件届くたびに Context 全体が変わり `CollapsibleAnalysis` が全件浅く再描画される | 新規。**7パネルでは無害。300パネルへ横展開する前に per-panel 購読へ**（§0.6⑤） |
| **FU36** | S18 が足した `DirectionGlyph` **184箇所すべてで `eps` を渡していない**。色側に不感帯がある箇所（勝率 0.48–0.52、`signShare`/`posShare` 0.4–0.6、`\|ic\|<0.1`、`PersistenceChart` の相関）で**色は灰色なのに記号だけが方向を主張する** | 新規 → **S20**。A4 の趣旨（色が見えなくても同じ結論）を壊す向き → §0.7② |
| **FU37** | 方向を持たない量に記号が付いた。最大ドローダウンは定義上つねに ≤0 なので **▼が常時点灯する**（`VolTargetingChart` ほか3箇所） | 新規 → **S20**。プロンプトが警告していた「記号が常に片側に倒れる」形 |
| **FU38** | **A3 の被覆が Canvas 150件中10件。棚卸し表と選定基準が無い** | 新規 → **S20**。S18 の受け入れ条件1・2 が未達。**棚卸しを第1コミットの条件にすること** → §0.7① |
| **FU39** | S18 が `chart-colors.ts` を使わず `#16a34a` / `#dc2626`（= `DIRECTION_COLORS` と同値）をリテラルで複製した（3ファイル） | 新規 → **S20**。FU29 と同型。FU12 が集約したものを崩す向き |
| **FU40** | sitemap の手動台帳（`PRICE_UNAVAILABLE_TICKERS`）が腐る問題は未解決。`ticker-pages.test.ts` が固定しているのは `9613` と件数下限の2点だけ | 新規。**新たに取得不能になった銀柄は検知しない** → §0.7⑤ |
| **FU41** | `AccordionSection` の React key に `summaryScope` を含めたため、銀柄・期間・最終足が変わるたび全パネルが再マウントされる | 新規 → **S19 で FU35（per-panel 購読）と同時に見直す** → §0.7④ |

---

## 変更履歴

| 日付 | 内容 |
|---|---|
| 2026-08-15 | 初版。ラウンド4（S12・S13）のマージ完了を受けて作成 |
| 2026-08-17 | §0 の ①②③ を対処済みとして書き換え（`cf09c48` / `7126c26` / `6378813`）。§1 から解決済みの前提を外し、§5 の項目を実態に合わせた。FU26・FU27 を追加 |
| 2026-08-21 | 第1波（S14 / S16）をレビューして main へマージ（`b07d684` / `339a8ba`）。追い修正 `bdedcd9`。§0.5 を新設（実測値・6つの学び）。§1 の波を更新し S17 / S18 / S19 のプロンプトを追加。§2 に「分岐点を古くしない」を追加。§4 の手順にマージ前の整合確認を追加。§5 に ⑧〜⑪、§6 に FU28〜FU31 を追加 |
| 2026-08-23 | push 後の本番確認を実施し §5① に実測結果を記録（**不具合なし**）。FU26 の調査が済んだこと・新規 FU32 を §5①' に追記 |
| 2026-08-23 | 第2波（S15）をレビューして main へマージ（`fe392ab`）。§0.6 を新設（変更点7つ・学び5つ）。§1 の波を第3波（S17 / S18 並列）へ進め、§2 の worktree を2本に更新。S17 / S18 / S19 のプロンプトに S15 の結果を反映。FU24 を S19 へ移し、FU30 を一部完了に。新規 FU33 / FU34 / FU35 |
| 2026-08-28 | 第3波（S17 / S18）をレビューして main へマージ（`b8a571e` / `c3c568f`）。§0.7 を新設（変更点7つ・学び07つ）。**S18 の A3 は 150件中10件なので「着手」扱いとし、残りを S20 として §3 に新規追加**。§1 を第4波（S19 → S20）へ、§2 の `$pairs` を S19 単独へ。S19 のプロンプトに第3波の申し送り3件を反映。§5 に本番確認リスト①''' を追加。新規 FU36〜FU41 |
